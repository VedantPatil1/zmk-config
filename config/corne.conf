# Battery reporting interval (seconds)
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=60

# Key debounce
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# BLE power boost (helps left-right communication)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Keyboard name shown in BLE
CONFIG_ZMK_KEYBOARD_NAME="Corne"

# Windows battery report fix
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# OLED display
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=y

# Sleep / Idle
# 10 minutes idle before entering idle state
CONFIG_ZMK_IDLE_TIMEOUT=600000
# Enable deep sleep
CONFIG_ZMK_SLEEP=y
# 15 minutes idle before entering deep sleep
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# RGB underglow
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_BACKLIGHT_AUTO_OFF_IDLE=y
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
# Default effect (3 = breathing)
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=3
# Max brightness (0-100, lower = better battery)
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MAX=60
# Don't use external power for RGB
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n
